name: project_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  curved_navigation_bar: ^1.0.6
  introduction_screen: ^4.0.0
  google_fonts: ^6.3.1
  smooth_page_indicator: ^1.2.1
  shared_preferences: ^2.5.3
  flutter_polyline_points: ^3.0.1
  google_maps_flutter: ^2.13.1
  location: ^8.0.1
  flutter_svg: ^2.2.1
  path_drawing: ^1.0.1
  xml: ^6.5.0

  provider: ^6.1.5+1
  device_preview: ^1.3.1

  # FlutterFire (ให้เข้าชุดกันทั้งหมด)
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.3     # ⬅️ อัปให้เข้าชุดกับ core/auth/firestore
  google_sign_in: ^6.2.1
  flutter_image_compress: ^2.3.0
  cached_network_image: ^3.3.1
  

  # เลือกเวอร์ชันเดียวเท่านั้น ห้ามซ้ำ
  image_picker: ^1.1.2          # ⬅️ คงเหลือเวอร์ชันนี้ (ลบบรรทัด ^1.0.7 ออก)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/images/
    - lib/gifs/
    - lib/maps/
